# Assignments instead of comparisons

When using `=` instead of `==`, e.g. in a print statement, the value returned is not whether the two values are equal, but the value of the right-hand side of the assignment.

```java
int x = 5;
int y = 5;

System.out.println(x = y); // 5
```