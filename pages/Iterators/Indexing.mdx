# Preventing IndexOutOfBoundsExceptions

When iterating over an array or collection, take extra care with the loop variable and prevent accessing illegal indices.
This also applies for `substring()` operations on strings.