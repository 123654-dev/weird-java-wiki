# Printing objects

When printing objects in Java, you print the memory address of the object by default, not its value. However, **if** the object has a `toString()` method, it is internally invoked by `System.out.println()`, thus, in this case, the actual value is outputted.